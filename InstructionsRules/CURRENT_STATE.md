# Поточний Стан Проєкту (CURRENT STATE)

> **Останнє оновлення:** 17 липня 2026
> **Цільовий етап:** Хвиля 8 (Стабілізація та E2E Тестування)

Цей файл слугує точкою входу для будь-якого ШІ-агента або нового розробника, щоб миттєво зрозуміти, на якому етапі знаходиться розробка і що саме робиться просто зараз.

## 📍 Де ми зараз?
Ми успішно завершили **Хвилі 0-7.5**.
*   **Актуальна фаза:** Хвиля 7.5 (Reset & Seed) закрита. База повністю очищена від тестового сміття та заповнена еталонними даними.
*   **Статус:** Готуємося до виконання Хвилі 8 (E2E тестування та стабілізація). Хвиля 7 (Рекламації) імплементована через прапорець `is_reclamation_frozen` та дочірні замовлення.
*   **Наступні кроки:** Провести наскрізний аудит, закрити всі виявлені баги (напр. 42703 `next_activity_at`, UI модалки) та E2E-дірки перед переходом до Pre-Prod (Хвиля 9).

### Що вже готово:
- **GAP Analysis (v2.0):** План міграції на Плоску Модель статусів розбито на 10 Хвиль. Хвиля 2 скасована (двошарова модель відхилена).
- **Хвиля 0-1:** Інкапсуляція бази, перехід на RPC, ролі та доступи (RLS).
- **Хвиля 3-4:** Розбиття заміру/монтажу, регресії, SLA зсуви, AppSheet Webhook, Активності.
- **Хвиля 5:** Макро-етап «Доставки». Таблиці логістики, Дашборд логіста, розгалуження BY_DRAWING та самовивозу.
- **Хвиля 6:** Конструктив (ENGINEERING_QUEUE → IN_PROGRESS → NESTING). Персональний Kanban-борд.
- **Хвиля 7:** Механізм Рекламацій. Дочірні замовлення з `parent_order_id` та "заморозка" через `is_reclamation_frozen`.
- **Хвиля 7.5:** Скрипти `reset_test_data.sql` та `seed_reference_data.sql`.
- **Стек:** React + Vite + TypeScript (Frontend), Supabase (Backend). Vanilla CSS + Radix UI.
- **Audit Log:** Всі зміни пишуться через тригер `log_changes()`.

## 👥 Еталонні Дані (Після Seed)
У базі 9 еталонних співробітників (пароль для всіх `password123`):
1. `disp.kyiv@test.com` (Диспетчер Київ)
2. `disp.lviv@test.com` (Диспетчер Львів)
3. `measurer1@test.com` (Замірник Іван)
4. `measurer2@test.com` (Замірник Петро)
5. `eng1@test.com` (Конструктор Олексій)
6. `eng2@test.com` (Конструктор Марія)
7. `driver1@test.com` (Водій Василь)
8. `install1@test.com` (Монтажник Андрій)
9. `admin@test.com` (Super Admin)

Замовлення: 3 стартових замовлення (в різних статусах).

## 🛠 Поточна (Наступна) Задача
**Старт Хвилі 8: E2E Тестування та Стабілізація.**
1. **Аудит фактичного стану:** Заміна нативних prompt на Radix UI модалки, виправлення помилки OrdersList.
2. **E2E-прохід:** Проходження тестових замовлень усіма шляхами (Happy Path, регресії, рекламації).

## 📝 Беклог / Техборг та Зауваження
1. **Колонка next_activity_at:** Помилка у фронті 42703 (RPC vs колонка).
2. **Модель прав (RLS §1.14):** Status quo, чекає рішення (Хвиля 9).
3. **Хвиля 8 (Борг по Хвилях 6-7):** KPI конструктора, повноцінна обробка PAUSED в задачах.
4. **Тип resume_date:** Перевести з `date` на `timestamptz`.

## ⚠️ Важливий Контекст для Агентів
- Уся документація зведена до факту у `order_state_machine.md`, `order_stages_and_pause_rules.md`, та `DB_SCHEMA_ACTUAL.md`.
- Правила для ШІ та суворі обмеження стеку зафіксовані у `00_AI_DEVELOPMENT_MANIFEST.md` та `AGENTS.md`.
- **Заборонено використовувати Tailwind**. Всі стилі пишемо на Vanilla CSS/CSS Modules + Radix UI.
- Не порушуйте RLS: фронтенд ніколи не змінює статуси/дані напряму, тільки через RPC.
