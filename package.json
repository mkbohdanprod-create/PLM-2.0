{
  "name": "plm-module",
  "version": "1.0.0",
  "description": "PLM Module Monorepo",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "supabase:start": "npx supabase start",
    "supabase:stop": "npx supabase stop"
  },
  "dependencies": {
    "localtunnel": "^2.0.2",
    "pg": "^8.22.0"
  }
}
